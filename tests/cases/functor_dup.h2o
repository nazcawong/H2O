trait Functor[F[_]]:
    def map[A, B](fa: F[A], f: A -> B) -> F[B]

impl Functor[List]:
    def map(xs, f):
        map(xs, f)

impl Functor[List]:
    def map(xs, f):
        map(xs, f)

def main():
    println("x")
